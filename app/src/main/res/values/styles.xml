<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="NoteButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:paddingTop">15sp</item>
        <item name="android:paddingBottom">15sp</item>
        <item name="android:onClick">writeNote</item>
    </style>

    <style name="ExtraButton" parent="Widget.Design.FloatingActionButton">
        <item name="android:layout_marginEnd">3sp</item>
        <item name="android:layout_marginBottom">5sp</item>
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="backgroundTint">@color/colorPrimaryLight</item>
    </style>

    <style name="FrameExtraButton" parent="ExtraButton">
        <item name="android:clickable">false</item>
        <item name="android:focusable">false</item>
    </style>

</resources>
